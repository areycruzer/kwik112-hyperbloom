# Kwik 112 Round 2 Implementation Plan

**Goal:** Reframe the existing Pulse112 build as Kwik 112: a voice-first 112 call emulator backed by an auditable human dispatch console, then ship rubric-aligned public evidence and machine-readable discovery artifacts.

**Architecture:** Preserve the existing Hume/scripted voice station, local-create/model-refine call pipeline, dashboard state, decision timeline, reservation locks, and evaluation harness. Add only two small reusable browser contracts: live-call event payloads and deterministic pre-arrival guidance selection. Keep the public landing page server-rendered and link into the existing dashboard call station.

## Task 1: Lock safety contracts with tests

- Add failing tests for the three judge caller presets, conservative guidance selection, live-call payload grading, prompt boundaries, expanded Indian location cues, and fusion copy/gate invariants.
- Preserve critical recall and no-downgrade/prompt-injection tests.
- Run focused tests before implementation.

## Task 2: Voice-first call station

- Move caller presets into `lib/personas.ts` and expose Ramesh, John, and Sharma ji through the existing scripted-call controls.
- Reframe visible copy around placing a 112 call, retain MEASURED/SIMULATED provenance, and make voice failures route visibly to scripted playback.
- Publish finalized transcript turns through a typed browser event containing transcript, detected language, prosody provenance, and deterministic current grade.
- Tune scripted assistant lines and document the Hume prompt/config requirements for location-first, one-question-at-a-time language mirroring.

## Task 3: Dispatcher loop

- Consume the live-call event in the dashboard and render a compact LIVE 112 CALL strip while intake is active.
- Add deterministic “Pre-arrival guidance (dispatcher reads)” cards to incident detail.
- Tighten fusion, ETA, demo-guide, and audit copy without changing dispatch state semantics.

## Task 4: Engine hardening and evaluation

- Harden the model system prompt at both boundaries and preserve transcript delimiters/schema-only output.
- Extend spoken-location cues and only add Hinglish variants demonstrated by held-out misses.
- Re-run focused tests, full tests, local held-out evaluation, and fusion evaluation; commit fresh result artifacts and sync every public metric to the final outputs.

## Task 5: Public landing and identity

- Rename package and product-facing metadata to Kwik 112.
- Replace the root redirect with an SSR landing page containing identity, six-step flow, rubric evidence, verified benchmark block, human-baseline context, clear independence boundaries, JSON-LD, and links into the voice station/console.
- Preserve the console’s dense operational visual system; add a dashboard `noscript` summary and responsive mobile behavior.

## Task 6: Documentation and machine legibility

- Rebuild README around the six rubric headings, reproducibility, verified citations, benchmark artifacts, limitations, Round 2 changes, and truthful Codex/OpenAI contribution.
- Add a <=250-word abstract, `robots.ts`, `sitemap.ts`, `public/llms.txt`, metadataBase/canonical/OpenGraph, captions, and the <=120-second video script/transcript package.
- Add a raw-HTML assertion script for static rubric copy and discovery links.

## Task 7: Verification and external handoff

- Run full tests, strict TypeScript, production build, evaluations, raw-HTML checks, and `git diff --check`.
- Start the dev server and verify landing, call-station fallback, live monitor, incident guidance, and console on desktop and 375px mobile in Browser/IAB.
- Request independent code review and fix all blocking findings.
- Leave GitHub repository rename/deployment/upload as explicit external operations only when authentication and destination values are verified; never invent video or deployed URLs.
